\echo usage: stampede [command]
\echo
\echo Commands:
\echo
\echo 'migrate        Apply all migrations'
\echo 'unapply        Unapply all migrations'
\echo 'stomp          Unapply all migrations, then apply all migrations'
\echo 'drop           Reset database by dropping and creating the `public` schema'
\echo 'reset          Reset database by dropping and creating the `public` schema, then apply all migrations'
